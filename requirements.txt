# A2A Protocol - Core dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
sse-starlette>=2.1.0
httpx>=0.27.0

# Kubernetes agent
kubernetes>=31.0.0

# MCP tool layer (kubernetes-agent brain/dispatcher)
mcp>=1.2.0

# Slack bot
slack-bolt>=1.21.0
slack-sdk>=3.33.0

pymysql>=1.1.0           # Freshservice agent MySQL client (optional, legacy n8n tables)
psycopg2-binary>=2.9.9   # Freshservice agent PostgreSQL store (fw-noc MIM_Store cache)
