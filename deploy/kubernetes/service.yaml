apiVersion: v1
kind: Service
metadata:
  name: kubernetes-agent
  namespace: a2a-ops
  labels:
    app.kubernetes.io/name: kubernetes-agent
    app.kubernetes.io/part-of: a2a-ops
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kubernetes-agent
  ports:
    - name: a2a
      port: 8082
      targetPort: 8082
      protocol: TCP
