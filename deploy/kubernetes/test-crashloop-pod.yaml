apiVersion: v1
kind: Pod
metadata:
  name: test-crashloop
  namespace: a2a-ops
  labels:
    app.kubernetes.io/name: test-crashloop
    app.kubernetes.io/part-of: a2a-ops-demo
spec:
  restartPolicy: Always
  containers:
    - name: crash
      image: busybox:1.36
      command: ["sh", "-c", "echo starting && exit 1"]
